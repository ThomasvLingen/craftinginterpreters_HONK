class ButtonWithSound
{
    var sound;

    init(sound)
    {
        this.sound = sound;
    }

    press()
    {
        println(this.sound);
    }
}

var the_button = ButtonWithSound("That was easy");
the_button.press();
